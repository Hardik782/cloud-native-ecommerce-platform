apiVersion: apps/v1
kind: Deployment
metadata:
  name: products
  namespace: ecommerce
spec:
  replicas: 2
  selector:
    matchLabels:
      app: products
  template:
    metadata:
      labels:
        app: products
    spec:
      containers:
        - name: products
          image: <AWS_ACCOUNT_ID>.dkr.ecr.us-east-1.amazonaws.com/products:latest
          ports:
            - containerPort: 8000
          env:
            - name: SERVICE_NAME
              value: "products"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: ecommerce-secrets
                  key: PRODUCTS_DB_URL
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: products
  namespace: ecommerce
spec:
  selector:
    app: products
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP